
'use server';
console.log("[File: activeRydActions.ts] File loaded on server (Content significantly reduced after API route implementation).");

// The createActiveRydForEventAction_Step1 server action has been removed
// as its functionality is now handled by the /api/offer-drive API route
// using the Firebase Admin SDK.

// You can keep other server actions in this file if needed for other features,
// or remove the file if it's no longer used.
// For now, we'll leave it with just the console log.

export {}; // Add an empty export to make it a module if no other exports exist
